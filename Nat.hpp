#ifndef NOVA_HEADER_Nat
#define NOVA_HEADER_Nat

#include <cstdint>

namespace nova {
    using Nat = ::std::uintptr_t;

    using Nat8 = ::std::uint8_t;
    using Nat16 = ::std::uint16_t;
    using Nat32 = ::std::uint32_t;
    using Nat64 = ::std::uint64_t;
}

#endif
